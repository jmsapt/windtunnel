//! Provides a slice viewing plugin, that visualizes slices of the velocity or pressure fields

use bevy::prelude::*;

pub struct SliceViewerPlugin;

impl Plugin for SliceViewerPlugin {
    fn build(&self, app: &mut App) {
        todo!()
    }
}

pub struct TransparentCloudPlugin;

impl Plugin for TransparentCloudPlugin {
    fn build(&self, app: &mut App) {
        todo!()
    }
}
